# Step2 v2.0 业务逻辑重构文档

## 🎯 重构概述

根据新的业务定义，完全重构了Step2的绘制逻辑，从"窗扇(Panes)"模式升级为"梃+区域(Mullions+Units)"模式，实现更符合实际门窗设计流程的交互体验。

---

## 📋 核心业务定义

### 1. 框架定义 (Frame)
- **外框尺寸锁定**：由用户在Step1输入的总宽度(W)和总高度(H)定义
- **不可修改**：一旦进入Step2，外框尺寸即被锁定
- **边界限制**：所有后续操作都必须在此框架内进行，不能超出

### 2. 梃（Mullion）系统
- **定义**：梃是窗框内的分隔条，用于将窗户分割成不同的区域
- **类型**：
  - 垂直梃（竖梃）：纵向分隔
  - 水平梃（横梃）：横向分隔
- **功能**：每添加一个梃，原有的区域被分割成多个新的独立区域
- **自动计算**：系统自动计算每个新区域的内部净尺寸

### 3. 区域 (Unit) 系统
- **生成方式**：由梃的位置自动计算生成
- **类型配置**：
  - **[A] 封窗**：固定窗，不可开启
  - **[B] 开启窗**：可开启的窗扇
- **开启窗配置**：
  - **开启方式**：平开、推拉、内倒等
  - **开启方向**：左开、右开、上开、下开等
  - **可视化表示**：在2D图上用箭头或图标表示开启方向

### 4. 配件系统
- **格栅**：绘制在玻璃上的装饰线条
  - 支持样式、材质、颜色选择
- **护栏**：窗户底部的安全护栏
  - 支持样式、材质、尺寸配置
  - 提示安全规范
- **纱窗**：防蚊虫的纱网
  - 支持内层/外层位置选择
  - 在画布上表现为不同层级

### 5. 玻璃配置
- **类型选择**：单玻、双层中空、Low-E、夹胶玻璃等
- **用于BOM生成**：自动计算物料清单

---

## 🔄 数据结构变更

### 旧数据结构（v1.x）

```typescript
interface Pane {
  id: number
  x: number
  y: number
  width: number
  height: number
  isOpenable: boolean
  hasScreen: boolean
  hasGrille: boolean
  // ...
}
```

**问题**：直接操作窗扇，无法表现梃的概念，不符合实际设计流程。

### 新数据结构（v2.0）

```typescript
// 梃（分隔条）
interface Mullion {
  id: number
  type: 'vertical' | 'horizontal'
  position: number // 相对于框架的位置（mm）
}

// 区域（由梃自动计算生成）
interface Unit {
  id: number
  x: number
  y: number
  width: number  // 自动计算
  height: number // 自动计算
  
  // 区域类型
  type: 'fixed' | 'openable'
  
  // 开启窗配置
  openingType?: 'casement' | 'sliding' | 'tilt' | 'fixed'
  openingDirection?: 'left' | 'right' | 'top' | 'bottom'
  
  // 玻璃类型
  glassType: 'single' | 'double' | 'low-e' | 'laminated'
  
  // 配件
  hasScreen: boolean
  screenPosition?: 'inside' | 'outside'
  hasGrille: boolean
  grilleStyle?: string
  hasGuardrail: boolean
  guardrailHeight?: number
}
```

---

## 🎨 界面交互变更

### 顶部工具栏

**新增功能**：
- ➕ **竖梃按钮**：进入添加垂直梃模式
- ➕ **横梃按钮**：进入添加水平梃模式
- **外框尺寸显示**：强调框架尺寸已锁定（蓝色加粗）

**交互流程**：
1. 点击"竖梃"或"横梃"按钮
2. 提示："点击画布添加垂直/水平梃，点击任意梃可删除"
3. 在画布上点击，自动添加梃
4. 系统自动重新计算所有区域

### 左侧配置面板

**旧版（v1.x）**：
- 横截面示意图
- 当前窗扇信息
- 执手、纱窗、格栅配置

**新版（v2.0）**：
- **当前区域信息**：显示尺寸和位置
- **区域类型**：封窗 / 开启窗（强制选择）
- **开启方式**：平开 / 推拉 / 内倒 / 固定
- **开启方向**：左 / 右 / 上 / 下
- **玻璃类型**：单玻 / 双层中空 / Low-E / 夹胶玻璃
- **配件选项**：纱窗 / 格栅 / 护栏

### 画布渲染

**梃的渲染**：
- 垂直梃：灰色竖条，宽2px，高度铺满
- 水平梃：灰色横条，宽度铺满，高2px
- 悬停效果：变红色，提示可删除
- 点击删除：弹出确认框

**区域的渲染**：
- **封窗**：灰色边框 + 蓝色半透明玻璃
- **开启窗**：绿色边框 + 开启方向箭头（大号，半透明）
- **选中状态**：蓝色高亮边框 + 发光效果
- **区域编号**：左上角圆形徽章（封窗=灰色，开启窗=绿色）

**配件渲染**：
- **纱窗**：网格纹理，支持内外层级（z-index）
- **格栅**：九宫格分隔线（1/3和2/3位置）
- **护栏**：底部栏杆图案（5根竖条）

**尺寸显示**：
- 选中时顶部显示：`宽×高 | 类型 | 开启方式`
- 例如：`1200×1800mm | 开启窗 | 平开`

---

## 🔧 核心算法

### 区域自动计算算法

```typescript
const recalculateUnits = (mullions: Mullion[]) => {
  // 1. 获取所有垂直梃和水平梃的位置
  const vPositions = [0, ...mullions
    .filter(m => m.type === 'vertical')
    .map(m => m.position)
    .sort((a, b) => a - b), canvasWidth]
  
  const hPositions = [0, ...mullions
    .filter(m => m.type === 'horizontal')
    .map(m => m.position)
    .sort((a, b) => a - b), canvasHeight]
  
  // 2. 生成所有区域（笛卡尔积）
  const units = []
  for (let i = 0; i < hPositions.length - 1; i++) {
    for (let j = 0; j < vPositions.length - 1; j++) {
      units.push({
        id: unitId++,
        x: vPositions[j],
        y: hPositions[i],
        width: vPositions[j + 1] - vPositions[j],  // 自动计算
        height: hPositions[i + 1] - hPositions[i], // 自动计算
        type: 'fixed',
        glassType: 'double',
        // ...
      })
    }
  }
  
  setUnits(units)
}
```

**算法特点**：
- **自动化**：添加/删除梃后自动重新计算
- **准确性**：确保区域尺寸准确无误
- **边界处理**：自动包含框架边界（0和canvasWidth/Height）

---

## 🎬 交互流程示例

### 场景：设计一个2×2的四扇窗

1. **选择窗型**：选择"推拉窗 2扇"（初始化1条垂直梃）
2. **添加横梃**：
   - 点击"➕ 横梃"
   - 在画布中间点击
   - 系统自动生成4个区域
3. **配置区域1（左上）**：
   - 点击左上区域
   - 左侧面板显示配置选项
   - 选择"开启窗" → "推拉" → "左开"
   - 添加"纱窗"
4. **配置区域2（右上）**：
   - 选择"固定窗"
   - 选择"Low-E玻璃"
5. **配置区域3（左下）**：
   - 选择"开启窗" → "平开" → "右开"
   - 添加"护栏"
6. **配置区域4（右下）**：
   - 选择"封窗"
   - 添加"格栅"

**最终效果**：
- 画布显示4个区域，各有不同配置
- 开启窗显示绿色边框和开启方向箭头
- 纱窗、格栅、护栏正确渲染

---

## 📊 功能对比表

| 功能 | v1.x（旧版） | v2.0（新版） |
|------|-------------|-------------|
| 基础单元 | 窗扇 (Panes) | 区域 (Units) + 梃 (Mullions) |
| 区域生成 | 手动创建 | 自动计算 |
| 尺寸调整 | 手动编辑 | 通过梃自动计算 |
| 框架限制 | 可超出 | 严格限制 |
| 区域类型 | 开启/固定 | 封窗/开启窗（强制选择） |
| 开启方式 | 无 | 平开/推拉/内倒 ✨ |
| 开启方向 | 无 | 左/右/上/下 + 箭头显示 ✨ |
| 玻璃类型 | 简单选择 | 详细配置（单玻/中空/Low-E/夹胶） ✨ |
| 纱窗位置 | 无 | 内层/外层 ✨ |
| 护栏功能 | 无 | 完整支持 ✨ |
| 配置方式 | 浮动工具栏 | 左侧配置面板 |
| 视觉表现 | 基础 | 增强（箭头、护栏、层级） |

---

## 🚀 技术实现亮点

### 1. 响应式计算
- 添加/删除梃后立即触发区域重算
- 确保区域始终与梃布局一致

### 2. 强类型约束
```typescript
type: 'fixed' | 'openable'  // 强制选择
openingType?: 'casement' | 'sliding' | 'tilt' | 'fixed'
openingDirection?: 'left' | 'right' | 'top' | 'bottom'
glassType: 'single' | 'double' | 'low-e' | 'laminated'
```

### 3. 可视化增强
- 开启方向箭头：大号、半透明、居中显示
- 纱窗层级：z-index区分内外
- 护栏渲染：栏杆图案写实

### 4. 交互优化
- 梃悬停变红，提示可删除
- 区域点击配置，左侧面板同步
- 添加梃模式提示清晰

---

## 📦 新增依赖函数

```typescript
// 梃管理
addMullion(type, position)
deleteMullion(mullionId)
recalculateUnits(mullions)

// 区域配置
handleUnitClick(unitId, event)
configureUnitType(unitId, type)
configureOpeningType(unitId, openingType)
configureOpeningDirection(unitId, direction)
configureGlassType(unitId, glassType)
toggleUnitAccessory(unitId, accessory)
```

---

## 🎯 用户体验提升

### Before (v1.x)
1. 选择窗型 → 手动调整窗扇尺寸 → 配置属性
2. 尺寸容易出错
3. 无法表现实际设计流程

### After (v2.0)
1. 选择窗型 → 添加梃分隔 → 自动生成区域 → 配置区域属性
2. 尺寸自动计算，准确无误
3. 完全符合实际门窗设计流程
4. 配置选项更丰富，表现更直观

---

## 🐛 已知限制

1. **梃的拖拽调整**：暂不支持拖拽梃调整位置（下个版本考虑）
2. **区域合并**：暂不支持合并相邻区域（需删除梃实现）
3. **复杂窗型**：暂不支持不规则窗型（如圆弧窗）

---

## 📝 后续计划

### v2.1 规划
- [ ] 梃的拖拽调整
- [ ] 区域合并功能
- [ ] 护栏高度可调
- [ ] 纱窗材质选择
- [ ] 格栅样式选择器
- [ ] 导出BOM清单

### v2.2 规划
- [ ] 3D预览集成
- [ ] 窗型模板保存
- [ ] 批量配置相同区域
- [ ] 智能推荐配置

---

## 📚 相关文档

- `STEP2_WINDOW_DESIGN_GUIDE.md` - Step2 产品文档
- `STEP2_V1.3_ENHANCEMENTS.md` - v1.3 功能增强
- `STEP2_V2.0_BUSINESS_LOGIC_REFACTOR.md` - 本文档

---

**更新日期**：2024-10-31  
**版本**：v2.0  
**状态**：✅ 已完成并测试  
**重大变更**：是

